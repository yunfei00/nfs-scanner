# Sprint 002 Task 02 - Realtime Canvas

## Goal

Implement the base `RealtimeCanvas` with `QGraphicsView` / `QGraphicsScene`.

## Scope

- Empty state
- Fit view
- Reset view
- Zoom
- Pan
- Background

## Acceptance

- `RealtimeCanvas` can be used by a Workspace tab.
- Zoom and pan are usable.
- `python -m compileall nfs_scanner` passes.
